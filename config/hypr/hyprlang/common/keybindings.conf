# Intentionally small. Existing distro/user keybindings remain authoritative.
# Project-specific bindings will be added here only after validation.

$terminal = ~/.config/jc-hyprland-dotfiles/bin/launch-foot.sh

bind = SUPER, RETURN, exec, $terminal