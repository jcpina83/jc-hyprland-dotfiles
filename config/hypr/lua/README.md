# Hyprland Lua

Reserved for the Hyprland 0.55+ configuration layer.

The current workstation runs Hyprland 0.54.x, so the active implementation lives in `../hyprlang/`.
The project keeps this boundary explicit so a future migration can happen without changing distro adapters, themes or host data.
